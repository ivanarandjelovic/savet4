define(["angular", "./controllers", "common"], (angular, controllers) ->
  mod = angular.module("live.routes", ["user.services", "savet4.common"]);
  mod.config(["$routeProvider", ($routeProvider) ->
    ])
   
  mod

)
